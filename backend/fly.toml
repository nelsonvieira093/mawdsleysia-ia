# fly.toml app configuration file
# https://fly.io/docs/reference/configuration/

app = "backend-silent-snowflake-7300"
primary_region = "gru"

[build]

[http_service]
  internal_port = 8000
  force_https = true

  # 🔴 AJUSTES CRÍTICOS (CORREÇÃO DO 502)
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

  processes = ["app"]

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 1024
